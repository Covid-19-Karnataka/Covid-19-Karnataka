name: My Workflow

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Create dist directory
      run: mkdir -p dist

    - name: Minify JavaScript files
      run: npx uglifyjs public/js/*.js -o dist/app.min.js
